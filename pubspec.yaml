name: youth_biomarkers_sdk
description: "Separated SDK for providing biomarkers data"
publish_to: none
version: 0.1.0
homepage:

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  biosensesignal_flutter_sdk:
    git:
      url: https://github.com/aitzaz-dev/biosensesignalsdk.git
      ref: master
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  permission_handler: ^11.3.1
  dart_code_metrics: ^5.6.0

flutter:
  assets:
    - assets/images/

